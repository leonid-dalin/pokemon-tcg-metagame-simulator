# Optional Limitless API access key for bounded ingestion.
# Set this from the pocket-tier-list repository .env file when deck catalogue access is needed.
# Never commit the value or print it in logs.
LIMITLESS_API_KEY=
